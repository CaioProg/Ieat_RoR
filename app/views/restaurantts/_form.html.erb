<%= form_with(model: restaurantt) do |form| %>
  <% if restaurantt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurantt.errors.count, "error") %> prohibited this restaurantt from being saved:</h2>

      <ul>
        <% restaurantt.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
